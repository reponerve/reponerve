# pkg/

This directory contains public, reusable packages that may be imported by external projects.

## Structure

- `pkg/models/` - Shared domain models (Decision, Intent, Fact, Event, etc.)
- `pkg/memory/` - Public memory abstractions
- `pkg/types/` - Reusable public types
- `pkg/sdk/` - Client SDK
