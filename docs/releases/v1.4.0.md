# RepoNerve v1.4.0

Release Date: 2026-06-24

Status: **Released**

---

# Summary

Minor release: **freshness doctor**, **scoped monorepo scan**, **Homebrew formula**, and doc alignment for install channels.

---

# Highlights

## RFC-007: Freshness Doctor

* `reponerve doctor` — workspace, scan state, git HEAD, code index, discipline policy checks
* MCP tool `doctor` (49 tools total)
* Structured `DoctorResult` with recommendations

## RFC-008: Scoped Monorepo Scan

* `reponerve scan --modules <module paths>`
* `reponerve scan --changed` — modules from git diff paths
* Partial code index merge via `MergeCodeIndexModules`

## Homebrew

* Formula at `packaging/homebrew/reponerve.rb`
* Checksum helper: `scripts/homebrew-sha256.sh v1.4.0`

## Install channels (complete)

| Channel | Status |
| --- | --- |
| Install script | ✅ |
| GitHub Releases | ✅ |
| npm | ✅ package (publish requires `NPM_TOKEN`) |
| Homebrew | ✅ formula (tap publish optional) |

---

# Upgrade

```bash
curl -fsSL https://raw.githubusercontent.com/reponerve/reponerve/main/scripts/install.sh | bash
# or
npm install -g reponerve@1.4.0
# or
reponerve doctor --json
```

---

# Related

* `docs/rfc/RFC-007-freshness-doctor.md`
* `docs/rfc/RFC-008-scoped-monorepo-scan.md`
* `docs/install.md`
